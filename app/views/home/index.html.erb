<% if current_user %>
	<h1>Welcome Back</h1>
<% else %>
	<h1>Welcome, Sign Up to Join!</h1>
	<%= link_to "Sign Up", new_user_path, :class => "button" %>
	<%= link_to "Log In", new_session_path, :class => "button" %>
<% end %>
